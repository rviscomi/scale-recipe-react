{
  "name": "scale-recipe-react",
  "version": "1.0.0",
  "description": "Scaling a recipe up/down with React.",
  "author": "Rick Viscomi (rviscomi@gmail.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rviscomi/scale-recipe-react.git"
  },
  "bugs": {
    "url": "https://github.com/rviscomi/scale-recipe-react/issues"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t [ babelify ] js/recipe.js -o dist/recipe.js",
    "deploy": "git subtree push --prefix dist origin gh-pages",
    "watch": "watchify -v -t [ babelify ] js/recipe.js -o dist/recipe.js"
  },
  "devDependencies": {
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "react": "^15.3.0",
    "react-dom": "^15.3.0",
    "watchify": "^3.7.0"
  }
}
